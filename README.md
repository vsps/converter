A batch converter based around ImageMagick and FFMPEG.
Designed for stills and video.

This tool scans all possible output options and creates an easy to navigate arguments list to give the full control of a command line program with the ease of point and click.
All settings persist in json files.

Written with [entirely] Claude so "at your own risk" applies more than ever.
